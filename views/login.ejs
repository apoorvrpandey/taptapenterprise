<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TapTap Enterprise</title>
    <link rel="icon" type="image/x-icon" href="img/sidebarlogo.png" />
    <link rel="stylesheet" href="styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    
    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: 'Roboto', Arial, sans-serif;
        }

        .container {
            display: flex;
            height: 100%;
        }

        .left {
            background-color: white;
            flex: 5; /* Updated to take 70% of the space */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .right {
            background-color: white;
            flex: 5; /* Updated to take 30% of the space */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        h1 {
            margin: 0;
            padding: 20px;
        }

        p {
            padding: 20px;
        }

        .google-btn {
            background-color: white;
            border: 2px solid rgb(17, 26, 78);;
            box-shadow: rgba(78, 37, 0, 0.12) 4px 7px 7px 0px;
            color: rgb(17, 26, 78);;
            padding: 15px 50px;
            text-align: center;
            text-decoration: none;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            font-size: 16px;
            margin: 4px 2px;
            transition-duration: 0.4s;
            cursor: pointer;
            border-radius: 30px;
            width: 250px;
        }

        form {
            display: flex;
            flex-direction: column;
            width: 100%;
            max-width: 350px;
            padding-right: 50px;
        }

        label {
            margin-bottom: 8px;
            color: black;
            font-size: 16px;
        }

        input[type="email"], input[type="password"] {
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            width: 100%;
            box-sizing: border-box;
            background-color: white;
            transition: border-color 0.3s;
            margin-top: 6px; /* Added to provide space between label and input */
        }

        input[type="email"]:focus, input[type="password"]:focus {
            border: 1px solid #ccc; /* Change this color as needed */
            outline: none;
        }

        button[type="submit"] {
            background-color: rgb(192, 255, 114);
            border: 1px solid rgb(192, 255, 114);
            box-shadow: rgba(78, 37, 0, 0.12) 4px 7px 7px 0px;
            color: black;
            padding: 15px 170px;
            text-align: center;
            text-decoration: none;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 16px;
            margin: 4px 2px;
            transition-duration: 0.4s;
            cursor: pointer;
            border-radius: 30px;
            width: 250px;
            font-weight: bold;
        }

        button[type="submit"]:hover {
            background-color: rgb(192, 255, 114);
            font-weight: bold;
        }

        .hr-or {
            display: flex;
            align-items: center;
            text-align: center;
            width: 100%;
            margin: 20px 0;
            color:rgb(217, 217, 217);
        }

        .hr-or::before, .hr-or::after {
            content: '';
            flex: 1;
            border-bottom: 1px solid #ccc;
        }

        .hr-or:not(:empty)::before {
            margin-right: .24em;
        }

        .hr-or:not(:empty)::after {
            margin-left: .24em;
        }

        .google-btn img {
            width: 20px;
            height: 20px;
            margin-right: 8px;
        }

        p {
            font-size: 24px;
            font-weight: 700;
            text-align: center;
            line-height: 28.13px;
            color: rgb(17, 26, 78);
            padding: 0;
        }

        .toast {
            visibility: hidden;
            max-width: 300px;
            height: 50px;
            margin: auto;
            background-color: #ffdbac;
            color: red;
            text-align: center;
            border-radius: 8px;
            border: 1px solid red;
            position: fixed;
            z-index: 1;
            left: 0;
            right: 0;
            top: 30px; /* Adjusted from bottom to top */
            font-size: 17px;
            white-space: nowrap;
        }

        .toast #desc {
            color: red;
            padding: 16px;
            overflow: hidden;
            white-space: nowrap;
        }

        .toast.show {
            visibility: visible;
            -webkit-animation: fadein 0.5s, fadeout 0.5s 2.5s;
            animation: fadein 0.5s, fadeout 0.5s 2.5s;
        }

        @-webkit-keyframes fadein {
            from {top: 0; opacity: 0;} /* Adjusted from bottom to top */
            to {top: 30px; opacity: 1;} /* Adjusted from bottom to top */
        }

        @keyframes fadein {
            from {top: 0; opacity: 0;} /* Adjusted from bottom to top */
            to {top: 30px; opacity: 1;} /* Adjusted from bottom to top */
        }

        @-webkit-keyframes fadeout {
            from {top: 30px; opacity: 1;} /* Adjusted from bottom to top */
            to {top: 0; opacity: 0;} /* Adjusted from bottom to top */
        }

        @keyframes fadeout {
            from {top: 30px; opacity: 1;} /* Adjusted from bottom to top */
            to {top: 0; opacity: 0;} /* Adjusted from bottom to top */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="left">
            <img src="img/loginPageImage.png" alt="logo" width="85%" height="90%" style="padding-left: 50px;">
        </div>
        <div class="right">
            <form action="/login" method="post">
                <div style="text-align: center ;margin: 5px;">
                    <img src="img/sidebar logo.png" alt="logo" width="25%" height="95%" style="align-items: center;">
                </div>
                <p>Achieving Excellence Together</p>
                <div>
                    <label for="username">Username</label>
                    <input type="email" id="username" name="username" required>
                </div>
                <div>
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <div style="align-items: center;text-align: center;"><button type="submit">Login</button></div>
                <div class="hr-or">OR</div>
                <a href="/auth/google" class="google-btn"><img src="img/google.png" alt="Google Logo"> Log In Using Google</a>
            </form>
        </div>
    </div>

    <div id="toast" class="toast">
        <div id="desc">Invalid Username or Password</div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const urlParams = new URLSearchParams(window.location.search);
            const error = urlParams.get('error');
            if (error === 'invalid_credentials') {
                const toast = document.getElementById('toast');
                toast.classList.add('show');
                setTimeout(() => {
                    toast.classList.remove('show');
                }, 3000);
            }
        });
    </script>
</body>
</html>
